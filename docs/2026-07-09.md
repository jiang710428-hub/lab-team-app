# 2026-07-09 工作日志

## Bug修复 & 交互调整
1. material.html：取消右上角"..."的点击事件（已开发完成），移除 sheetMore 弹窗和 more() 函数
2. material-detail.html：修复返回按钮无效问题
   - 原因：`<a>` 标签无 `href` 属性，部分浏览器不响应 onclick
   - 修复：改为 `<div>` 元素（与 warehouse-detail.html 返回按钮一致）
   - goBack() 逻辑不变：根据 from 参数动态返回（warehouse-detail→仓库详情，否则→物料列表）
3. material-transfer.html：目标仓库选择从下拉改为底部弹窗
   - 移除 dropdown 相关 CSS/HTML/JS
   - 新增 wh-select 触发器 + 底部 ActionSheet（mask+sheet）
   - 弹窗标题"选择目标仓库"，列表项含仓库名/编号/负责人 + 选中勾
   - 取消按钮独立卡片在弹窗下方
4. 路由历史栈堆积 Bug 修复：
   - 问题：仓库详情→物料详情→返回仓库详情→返回，结果跳到物料详情页
   - 原因：material-detail.html 的 goBack() 用 location.href（添加新历史记录），warehouse-detail.html 返回用 history.back()（只回退一步），导致回退到物料详情页
   - 修复1：warehouse-detail.html 返回按钮改为 `location.href='warehouse.html'`（直接跳转，不依赖历史栈）
   - 修复2：material-detail.html 的 goBack() 改为 `history.back()`（回退一步即可，避免历史栈堆积）
   - 经验：页面间跳转应统一策略——要么全用 history.back()，要么全用 location.href 直跳，不能混用
5. 新增入库页 inbound.html：
   - 顶部导航：物料；5个 Tab（物料列表/入库/出库/类目/仓库），第5个 Tab 按用户要求把"出库统计"改为"仓库"
   - 搜索 + 筛选；筛选弹窗含"全部/本周/本月/本季度"
   - 入库总金额统计行
   - 入库记录卡片：物料名+右上角"..."（含查看详情/编辑/删除）、物料编号/入库数量/批次编号/入库时间、备注
   - 空状态提示"没有更多入库记录了"
   - 底部双按钮：扫码入库（绿色）+ 物料入库（蓝色）
   - 入库/出库/类目 Tab 为 toast 占位，"仓库" Tab 跳转 warehouse.html，"物料列表" 跳 material.html
6. 修复 material.html 中"入库"Tab 跳转，原 switchTab 走的是 else toast 分支
7. inbound.html 新增2条入库记录（实验用手套、离心管），入库总金额自动更新为 2350.00元
8. 新增入库录入页 inbound-entry.html（点击"扫码入库"进入）：
   - 顶部扫码输入框，placeholder 改为"扫描（主）物料码自动查询物料"
   - 表单：选择物料*(底部弹窗)/请选择批次*(弹窗)/单价*(数字输入)/数量/有效期至(日期选择器)/存放仓库(底部弹窗)/接收人(底部弹窗)/图片(0/9)
   - 备注卡：备注(文本输入)
   - 底部：本次入库合计(实时计算) + 确认入库
   - 右上角筛选图标(无点击事件)
   - 表单卡头部"物料" + 右上角红色删除图标
9. 新增物料入库录入页 inbound-material.html（点击"物料入库"进入）：
   - 与扫码入库页不同：无扫码输入框，有"+添加物料"按钮
   - 右上角筛选图标无点击事件
   - 表单字段同扫码入库页：选择物料*/请选择批次*/单价*/数量/有效期至/存放仓库/接收人/图片
   - "添加物料"按钮：点击动态新增物料卡，支持多物料同时入库
   - 每张物料卡可独立删除（红色删除图标）
   - 底部合计 = 所有卡片单价×数量之和
10. 扫码入库页 inbound-entry.html 字段交互调整：
    - "选择物料"去掉交互和右箭头图标（扫码自动带出，无需手动选择）
    - "请选择批次"去掉交互但保留右箭头图标
    - "接收人"去掉交互但保留右箭头图标
    - 移除对应的物料/批次/接收人弹窗和JS函数
    - 确认入库校验改为检查单价是否填写
12. 新增入库详情页 inbound-detail.html（点击入库页物料卡片进入）：
    - 初始版本含二维码卡片+基础信息+库存信息，后按用户要求调整：
    - 删除顶部二维码卡片
    - 基础信息→入库信息：删除有效期至/存放仓库字段，名称→物料名称，编号→物料编号，新增入库时间/入库数量/接收人/备注
    - 库存信息→入库清单：保留存放仓库/有效期至/批次编号 + 物料表
    - 物料表字段：序号/子编号/物料二维码/操作（下载二维码+详情）
    - 表格支持左右滑动
    - 详情操作：弹窗展示子物料详情
    - 通过 URL 参数 ?idx=N 传递记录索引，动态填充数据
13. inbound.html 修改：
    - 物料记录卡片添加 onclick 跳转 inbound-detail.html?idx=N
    - "..."按钮添加 event.stopPropagation() 防止冒泡触发卡片跳转
    - 删除操作改用自定义确认弹窗（替代 confirm()）
    - 卡片添加 :active 点击反馈

13. 新增出库页 outbound.html（5Tab布局：物料列表/入库/出库/类目/仓库，"出库"当前选中）：
    - 与入库页结构一致，统计行改"出库总金额"
    - 记录字段：物料名/物料编号/出库数量/领用人员/出库时间 + 备注
    - 底部双按钮：扫码出库（绿色）/ 物料出库（蓝色）
    - 3条数据：试剂(a3321,1,哈哈,2026-07-07,100元) + 实验用手套(c1025,5,扎西顿珠,2026-07-06,150元) + 离心管(c2078,22,次仁旺姆,2026-07-05,330元)
    - 出库总金额 = 580.00元
    - 修复 inbound.html "出库"Tab 跳转：toast → location.href='outbound.html'
    - 修复 warehouse.html "出库"Tab 跳转：toast → location.href='outbound.html'
    - 卡片点击跳转到 outbound-detail.html（占位URL，待后续设计详情页）
14. 入库详情页 inbound-detail.html 调整：
    - 删除顶部二维码卡片
    - 基础信息→入库信息：删除有效期至/存放仓库，名称→物料名称，编号→物料编号，新增入库时间/入库数量/接收人/备注
    - 库存信息→入库清单
15. material.html：取消"+添加物料"FAB按钮交互（移除onclick、sheetAdd弹窗、addWay函数）
16. inbound-detail.html：入库清单表格"详情"按钮改为跳转 sub-material-detail.html?no=A00X（原为弹窗展示）
17. sub-material-detail.html：详细信息中增加"库存状态"字段（在库=绿色，已出库=灰色），数据中A002/A003/A004为已出库

## 出库录入页 outbound-entry.html（2026-07-09 新增）v3
- 顶部扫码框 placeholder："扫描物料自动查询物料，并把物料合并后选择下方批"
- 表单字段：
  - 选择物料*（不可交互，无右箭头，默认显示"试剂"）
  - 选择仓库*（点击底部弹窗：乃东路一号库/泽当中心库/样品暂存库）
  - 选择批次*（点击底部弹窗：共选2个批次/仅p0003/仅pc0002，**保留右箭头**）
- 出库清单（**物料卡内嵌**），每行展示：
  - 第一行：批次号 + 「点击选择+右箭头」
  - 第二行：库存 / 已选 / 单价 三列（**总数字段名改为"库存"**；去掉"试剂.单价20元"行）
  - 点击进入 outbound-batch.html?bid=p0003 子页面勾选
- **整张大物料卡合并**：表单 + 出库清单 + 领用人 + 图片 + 周期性使用完 + 备注
  - 领用人*（点击底部弹窗：扎西顿珠/次仁旺姆/多吉/哈哈）
  - 图片（0/9 占位，选填）
  - 周期性使用完：是/否 单选（默认"否"，提示"如：物料A库存5瓶，已使用完1瓶"）
  - 备注
- 底部"本次出库合计" + "确认出库"（校验仓库/领用人/至少勾选一件）
- 数据共享：localStorage 'outbound_entry_state' 跨页面同步勾选状态；结构 {selections, currentBatches}
- 右上角图标无点击事件

## 出库清单子页 outbound-batch.html（2026-07-09 新增）v2
- 顶部批次信息卡：批次号 / 总数 / 已选 / 单价（**已去掉"试剂·单价20元"副标题**）
- 子编号搜索框（实时过滤）
- 表格：复选框（点击全选可见项）/序号/子编号/有效期至
- 有效期至列：失效=红色"失效"标 / 即将失效=橙色"即将失效"标
- 底部"本批小计" + "完成"按钮（自动保存+回退到出库录入页）
- p0003 单价20元 / pc0002 单价100元

## 路由修复
- 修复 material.html "出库"Tab 跳转：原走 else toast → 改为 location.href='outbound.html'

## 出库录入页迭代（v3 → v4）
18. outbound-entry.html 选择批次恢复右箭头（但无交互）
19. outbound-entry.html 领用人改为无交互但保留右箭头
20. outbound-entry.html 备注从物料卡拆出为独立卡片
21. outbound-batch.html 删除"试剂·单价20元"副标题
22. outbound-entry.html 出库清单每行改为两行布局：第一行批次号+点击选择，第二行库存/已选/单价三列（"总数"→"库存"）
23. 新增 outbound-material.html（物料出库录入页）：
    - 与扫码出库页相同结构，但**无扫码框**
    - 选择物料无交互但**保留右箭头**（扫码出库是物料无箭头）
    - **"+添加物料"按钮**在备注上方，可动态新增物料卡
    - 每张物料卡独立状态（wh/periodic/selections/currentBatches）
    - 删除用自定义确认弹窗（仅多卡时显示删除按钮）
    - 底部合计=所有卡片累加
    - localStorage: 'outbound_material_state'，结构 {cards:[{selections,currentBatches,wh,periodic}]}
24. outbound-batch.html 支持 ?source=material&idx=N 参数：
    - source=material → STORAGE_KEY='outbound_material_state'，读写 cards[idx].selections[bid]
    - 默认 → STORAGE_KEY='outbound_entry_state'，原逻辑
25. outbound.html "物料出库"按钮跳转 outbound-material.html
26. outbound-batch.html 增加筛选功能（搜索右侧下拉）：
    - 选项：全部 / 15天内失效 / 已失效（ActionSheet 弹窗）
    - 状态：currentFilter = 'all'|'warn'|'bad'，默认 'all'
    - render/toggleAll/updateSummary 共享 getVisibleItems() 同步过滤
    - 表格"即将失效"橙色标签已删除（用户要求），仅保留红色"失效"标签
    - 失效/即将失效的 status 字段仍在 BATCH_DATA 中保留，供筛选过滤使用
27. 新增 outbound-detail.html（出库详情页）：
    - 出库信息：物料名称/类目/物料编号/规格/出库时间/出库数量/单位/领用人/图片(0/9)/周期性使用完/备注
    - 出库仓库：蓝色图标+仓库名，点击跳 warehouse.html
    - 出库清单（按批次汇总）：序号/批次号/单价/出库数量
    - 出库表（子物料）：序号/子编号/物料二维码/操作(下载二维码 toast + 详情跳 sub-material-detail)
    - 通过 ?idx=0/1/2 选择数据（试剂/实验用手套/离心管）
    - 出库列表卡片点击和"查看详情"操作均跳转 outbound-detail.html
28. outbound-detail.html 样式重写（与入库详情页统一）：
    - 改用 .group + .group-title(灰色标题栏) + .row(标题靠左90px/内容靠右text-align:right)
    - 图片改为仅展示（缩略图占位，无"+"上传框）；周期性使用完改为仅展示文字（无单选按钮）
    - 出库仓库改为仅展示（去掉"查看›"链接和点击跳转）
    - 出库表按批次分组：每个批次有蓝色批次标题行 + 独立子物料表
    - 物料二维码列只显示二维码图标（qrMiniSvg），不再重复显示编号文字
    - 试剂(idx=0)设为2个批次(pc0002出1件+a3321-009 / p0003出2件+a3321-001+a3321-002)演示多批次
    - 操作按钮统一用 outline-blue(下载二维码) + outline-green(详情) 边框样式
29. 新增 me.html（个人中心页）：
    - 顶部蓝渐变背景 + 标题"我的"
    - 用户区：60px 圆角头像(SVG人像) + 姓名"南山花开" + 团队"前沿交叉学院团队"
    - 主菜单白卡（圆角合并）：扫一扫 / 我的预约 / 我的话题
      - 我的话题右侧副标题"你收到99+条新回复"（蓝色"99+"）
    - 帮助中心（4列网格）：版本介绍 / 近期更新 / 功能说明 / 关于我们
    - 其他（3列网格）：个人资料 / 绑定邮箱 / 修改密码 / 切换账号 / 使用协议 / 邀请链接 / 审计日志
    - 所有图标都是内嵌彩色 SVG 圆角
    - 底部 TabBar：首页(active home.html) / AI助手 / 资料库 / 我的(active)
    - 12 个菜单项点击全部 toast 占位（首页 Tab 跳 home.html）
30. home.html 底部 TabBar"我的"加 onclick=location.href='me.html'
31. 新增 scan.html（扫一扫页）：
    - 纯白背景 + 顶部状态栏 + 导航栏（返回箭头 + 标题"扫一扫"）
    - 居中 220px QR 码 SVG（手绘三定位角+随机数据小方块）
    - 四角青色 L 型装饰线
    - 底部提示文案："扫描设备、主/子物料、样品、实验记录等二维码查看信息"
    - 返回 history.back()
    - me.html 扫一扫点击改为 location.href='scan.html'
32. me.html 主菜单白卡"扫一扫"下方新增"扫码预约设备"行（蓝色设备+二维码SVG图标，点击toast占位）
33. 新增 my-appointment.html（我的预约页）:
    - 顶部状态栏+导航(返回+居中标题+右上蓝字胶囊"扫码预约"按钮[设备+二维码图标,toast占位])
    - 设备预约白卡:标题"设备预约"+"全部预约>"链接/头像+2025年8月月份(带下拉)/一-日7列表头/2025-08日历(8月1日是周五,补上7月28-31灰显)/15日绿点/21日选中(蓝色实心圆+白字)/22日橙点/29日蓝点/中心蓝渐变+悬浮按钮
    - 下方按日期分组的预约卡:08-27张三的预约(待开始[蓝])/08-21张三的预约(进行中[橙],有备注)/08-15张三的预约(已取消[灰])
    - 卡片字段:状态圆点+名称+右上状态胶囊/编号+设备名/备注(有则显示)/时段+右侧...菜单(toast占位)
    - 日历格子可点击切换选中态
34. me.html 调整:
    - 删除主菜单白卡中"扫码预约设备"行(已迁移到预约页右上角改名为"扫码预约")
    - "我的预约"行 onclick 改为 location.href='my-appointment.html'
