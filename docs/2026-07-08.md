# 2026-07-08 工作日志

## 页面还原
- home.html：还原App首页（状态栏+蓝渐变头部+8宫格功能区+双卡片公告+待办事项+科研笔记日志周视图+底部TabBar）
- material.html：还原物料列表页（返回/标题/更多 + 5Tab + 搜索筛选 + 物料卡片 + 添加悬浮按钮）

## 交互补全
- home.html：8宫格改为 `<a>`，物料格跳转 material.html，其余 alert 占位，加点击缩放反馈
- material.html：补全所有交互
  - 返回箭头 → `href="home.html"` 跳回首页
  - Tab → 切换高亮，物料列表渲染数据，其余提示待开发
  - 搜索 → 回车按名称/编号筛选
  - 筛选 → 底部ActionSheet（全部/低库存/即将失效/已失效）
  - 详情 → 居中Modal展示完整字段
  - 操作 → 底部ActionSheet（出库/入库/调拨/编辑/删除），删除真实移除卡片
  - 添加物料 → ActionSheet（手动录入/扫码/批量导入）
  - 更多 → ActionSheet（刷新/导出/设置）
  - 统一 Toast 提示 + 点击反馈

## 约定记录
- 用户要求：所有页面默认加完整交互，不需提醒。已写入 MEMORY.md

## 筛选功能升级
- 物料页"筛选"改为支持「类目」+「存放仓库」双维度多选筛选
- 底部弹出筛选面板：类目chips(试剂/耗材...) + 仓库chips(乃东路一号库/泽当中心库...)，可多选
- 底部"重置"+"确定"按钮
- 确定后实时刷新列表，筛选按钮显示红点提示，Toast反馈筛选结果数
- chip选项从数据动态生成(去重)，可扩展

## UI微调
- Tab栏改为flex:1水平均匀分布（去掉gap，每个Tab等宽居中）
- "部分库存失效"+"即将失效"两个标签合并为单个橙色标签"部分库存失效/即将失效"

## 物料详情页
- 新增 material-detail.html：物料详情页
  - 顶部：返回箭头 + "物料详情"标题
  - 二维码卡片：内嵌SVG二维码 + 蓝色"保存"按钮
  - 基础信息：名称/编号/类目/规格/单位/库存/预警值/备注（8行）
  - 物料自定义属性：空状态
  - 库存信息：存放仓库chip + 搜索框 + "全部"下拉 + 左右滑动表格
  - 表格列：序号/子编号/物料二维码/有效期至/批次号/单价/操作
  - 操作列含：下载二维码(蓝) + 详情(绿) 两个小按钮
  - 底部"左右滑动查看完整数据"提示
- 交互：返回跳回 material.html / 保存二维码 / 仓库切换 / 表格行搜索 / 行点击打开ActionSheet(下载二维码/查看详情) / 操作按钮独立点击 / 表格水平滑动
- material.html：列表卡片"详情"按钮改为跳转 material-detail.html
- 卡片图标改为更合适的"详情"图标(问号圆形)

## 详情页筛选调整
- 库存信息"全部"下拉改为失效状态筛选：全部/15天内失效/已失效
- 筛选与搜索联动（applyFilters统一处理关键词+失效状态）
- 有效期列按状态着色：已失效红色/15天内失效橙色/正常默认色
- 模拟数据更新为含三种状态的子物料（参考日期2026-07-08）

## 操作菜单
- 物料列表"操作"弹窗改为：详情 / 编辑 / 库存预警 / 删除(红)
- 取消按钮独立在底部，白底圆角，与上部分隔（sheet-cancel-out）
- 结构：sheet + 独立取消按钮 用 .sheet-group 包成一组，避免被 mask 的 flex-end 挤压变形
- 弹窗位于底部弹起（slideUp动画），不是居中
- 操作选项文字居中显示（justify-content:center），无图标，字号16px
- sheet-cancel-out 独立圆角卡片（border-radius:16px），与上方sheet有8px间隔

## 子物料详情页
- 新增 sub-material-detail.html
  - 顶部：返回箭头(用 history.back 回到详情页) + "子物料详情"标题
  - 详细信息组：存放仓库/批次号/单价/子编号/有效期至(失效标红+右侧"失效/即将失效/正常"标签)/二维码mini图(64px)+"下载二维码"按钮
  - 基础信息组：物料名称/编号/类目/规格/单位
  - 自定义属性空状态
- 通过 URL 参数 ?no=A00X 加载对应子物料数据
- material-detail.html：表格行操作列"详情"按钮 + ActionSheet中的"查看详情" 改为跳转到 sub-material-detail.html

## 仓库页
- 新增 warehouse.html
  - 顶部：← 返回(history.back) + "仓库"蓝色标题
  - 5个Tab：物料列表/入库/出库/类目/仓库(激活)；仓库Tab点击跳转到当前页
  - 搜索框（蓝边）支持按仓库名称/编号/地址/负责人 关键词搜索
  - 统计卡片3个：总仓库数(蓝) / 启用中(绿) / 存货总量(紫)
  - 仓库卡片：名称 + 启用/禁用tag + 编号 + 负责人/关联类型/地址 + 备注 + 库存总量；右上角编辑/删除icon按钮
  - 底部蓝色"+ 添加仓库"悬浮按钮
- 弹窗表单（新增/编辑通用）：
  - 字段：仓库名称* / 仓库编号(自动生成不可编辑) / 负责人* / 仓库地址* / 运行状态(启用/禁用 switch) / 备注说明
  - 仓库编号自动按 WH-NNN 生成，编辑时不可改
  - 校验：名称/负责人/地址 必填
- 删除：confirm 后真实从列表移除
- material.html：5Tab中的"仓库"Tab改为跳转到 warehouse.html
- 表单弹窗改为底部弹起（mask flex-end + slideUp + 顶部拖动条）
- 删除改为居中确认弹窗（confirm-mask），有库存(stock>0)的仓库不可删除，Toast提示原因

## 仓库详情页
- 新增 warehouse-detail.html
  - 顶部：← 返回(history.back) + "仓库详情"标题（无Tab导航，无更多按钮）
  - 仓库信息卡：名称+状态tag / 编号 / 负责人 / 关联类目 / 地址 / 备注+库存总量pill
  - 库存物料区：分区标题(库存物料，无种数统计) + 类目导航chips(全部/试剂/耗材...) + 蓝边搜索框 + 物料卡片列表
  - 物料卡片：绿色试剂瓶icon + 名称(无标签) + 2列字段网格(编号/类目/规格/库存) + 底部详情/调拨双按钮
  - 详情按钮 → 跳转 material-detail.html?from=warehouse-detail&whIdx=N（带来源参数）
  - material-detail.html 返回按钮改为 goBack() 函数：根据 from 参数动态跳转（warehouse-detail→回仓库详情，否则→回物料列表）
  - 调拨按钮 → 跳转 material-transfer.html?whIdx=N&matIdx=M（独立页面）
  - .phone改为flex布局，加.content可滚动区域修复页面无法上下滚动问题

## 物料调拨页（新增）
- material-transfer.html：独立调拨页面
  - 顶部：← 返回(history.back) + "物料调拨"标题
  - 目标仓库：下拉选择器样式，单选，仅显示启用的仓库（排除当前仓库），点击展开列表
  - 调拨范围：搜索框 + 表格(复选框+全选)，字段与物料详情页库存信息表一致（序号/子编号/物料二维码/有效期至/批次号/单价），支持左右滑动
  - 底部：已选N项统计 + 确认调拨按钮（选了物料+目标仓库才可点击）
  - 确认后Toast提示，1.2秒后history.back返回
  - 类目导航支持切换筛选，与搜索联动
- 通过URL参数 ?idx=N 定位仓库数据
- warehouse.html：仓库卡片整体可点击跳转到 warehouse-detail.html?idx=N，编辑/删除icon加stopPropagation避免冒泡
- "关联类型"统一改为"关联类目"（warehouse.html + warehouse-detail.html）
